function v({ o, b }) {
  Go(b, o);
}

function mb(o, a, b) {
  const ob = { ...o, v, o, b };
  Go(a, ob);
}

const o = {
  v(o, ...nargs) {
    console.log("v", nargs);
  },

  e(o, ...nargs) {
    console.log("e", nargs);
  },
};
function example(o) {
  /*** Left identity *******************************/
  /*** return a >>= f ≡ f a ************************/
  /**/ const ret = (o) => Go(o.v); /****************/
  /**/ const f = (o, a) => Go(o.v, a * 2); /********/
  /*************************************************/
  /**/ Go(mb, o, (o) => Go(ret, o, 3), f); /********/
  /*************************************************/
  /**/ Go(f, o, 3); /*******************************/
  /*************************************************/

  /*** Right identity: *****************************/
  /*** m >>= return ≡ m ****************************/
  /**/ const M = (o) => Go(ret, o, 6); /************/
  /*************************************************/
  /**/ Go(mb, o, M, ret); /*************************/
  /*************************************************/
  /**/ Go(M, o); /**********************************/
  /*************************************************/

  /*** Associativity *******************************/
  /*** (m >>= f) >>= g ≡ m >>= (\\x -> f x >>= g) **/
  /**/ const g = (o, b) => Go(o.v, b / 2); /********/
  /*************************************************/
  /**/ Go(mb, o, (o) => Go(mb, o, M, f), g); /******/
  /*************************************************/
  /**/ Go(mb, o, M, (o) => Go(mb, o, f, g)); /******/
  /*************************************************/
}
//example(o, [], 0);

function rg(o, a, b, c, d) {
  if (a === mb) Go(c, o, a, d, rg, d, mb, b);
  else Go(c, o, d, a, mb, d, mb, b);
}
module.exports = { mb, o, rg };
global.Σ = [];
global.α = 0;
